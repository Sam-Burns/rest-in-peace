default:

    suites:

        inline:
            paths:    [ %paths.base%/features/feature_files ]
            filters:  { tags: ~wip }
            contexts: [ InlineContext ]

        webserver:
            paths:    [ %paths.base%/features/feature_files ]
            filters:  { tags: ~wip&&@webserver }
            contexts: [ WebserverContext ]
